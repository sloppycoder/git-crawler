[DEFAULT]
some_key = some_value


[project.local]
local_path = will_be_replace_during_test
filter = repo*
type = BE

[project.remote]
group = mobilityaccelerator/ngcc_does_not_exist
type = FE

[project.1]
local_path = ~/Projects/ktb/next/gitlab/mirrors
filter = **/*.git
type = BE

